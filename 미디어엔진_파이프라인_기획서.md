# 미디어 엔진 (팀원4: 조경우) 파이프라인 기획서

> 이 문서는 AI 영상 가편집 프로젝트의 **팀원4 (미디어 엔진 엔지니어)** 담당 영역에 대한 기획서입니다.
> 클로드코드가 실제 구현에 착수하기 전, 어떤 이유로 이런 구조를 결정했는지까지 함께 정리했습니다.
> 다른 팀원(팀원1 프론트엔드, 팀원2 백엔드, 팀원3 AI 파이프라인) 담당 클로드가 연동점을 확인할 때도 참고할 수 있도록 작성했습니다.

---

## 1. 담당 범위 요약

팀원4는 실제 영상 파일을 물리적으로 자르고, 자막을 입히고, 최종 결과물을 인코딩하는 렌더링 엔진을 담당합니다. R&R 원문에는 MoviePy/FFmpeg 조합으로 명시되어 있었으나, 아래 §3에서 설명하는 이유로 **FFmpeg 단일 도구 체계**로 재설계했습니다.

---

## 2. 확정된 전체 파이프라인

```
Step 0: 입력 검증 (edit_data 스키마 유효성 확인)
Step 1: [FFmpeg] 컷 편집 → cut_only.mp4 (concat demuxer, -c copy 무손실 시도)
         └─ 실패 시(키프레임 불일치): 전체 재인코딩으로 fallback
Step 2: [timestamp_mapper] 컷 편집 후 기준으로 자막 타임스탬프 재계산
Step 3: [ass_generator] 편집 JSON + 스타일 프리셋 → .ass 자막 파일 생성
Step 4: [FFmpeg] cut_only.mp4 + .ass → subtitles 필터로 합성 → final_output.mp4
Step 5: 임시파일 정리 + 결과 경로 반환
```

## 3. 폴더/파일 구조

```
media_engine/
├── main.py                   # 팀원2 백엔드가 호출하는 유일한 진입점
├── cutter.py                  # FFmpeg 컷 편집 모듈 (concat demuxer, fallback 재인코딩)
├── ass_generator.py           # 편집 JSON + 스타일 프리셋 → .ass 자막 파일 생성
├── timestamp_mapper.py        # 컷 편집 후 자막 타임스탬프 재계산
├── style_presets.py           # 매운맛/순한맛/정석맛 스타일 프리셋 정의 (팀원3 연동점)
├── config.py                  # 폰트, 해상도, 경로 등 상수
├── exceptions.py              # 커스텀 예외 클래스
└── utils/
    ├── ffmpeg_wrapper.py       # subprocess 호출 래퍼 + stderr 파싱
    └── file_utils.py           # 임시파일/경로 관리
```

---

## 4. 주요 결정 사항과 근거

### 4-1. MoviePy 제거 → FFmpeg + ASS 자막으로 통일

**초기 계획**: 컷 편집은 FFmpeg, 자막 합성은 MoviePy를 쓰는 하이브리드 구조.

**변경 이유**: 처음엔 "자동 줄바꿈(caption wrap)"만 요구사항이었고, 이 경우 MoviePy `TextClip(method='caption')`이 FFmpeg `drawtext`보다 구현이 훨씬 간단했습니다. 그러나 이후 요구사항이 확장되면서(카라오케 하이라이트, 배경 박스, 외곽선/그림자 등) 상황이 바뀌었습니다.

- MoviePy로 이 모든 효과를 구현하려면 효과마다 별도 레이어(TextClip)를 만들어 CompositeVideoClip으로 합성해야 해서, 효과가 늘어날수록 코드 복잡도와 렌더링 시간이 함께 증가합니다.
- ASS(Advanced SubStation Alpha) 포맷은 애초에 이런 스타일링(카라오케 `\k` 태그, `BorderStyle=3` 배경 박스, 외곽선)을 위해 설계된 자막 규격이라, 줄바꿈까지 포함해 선언적으로 한 번에 처리됩니다.
- 결과적으로 컷 편집과 자막 합성이 전부 FFmpeg 하나로 통일되어, 의존성이 줄고 렌더링 속도도 빨라집니다(MoviePy는 프레임 단위 재처리가 느림).

**결론**: MoviePy는 파이프라인에서 완전히 제외합니다. R&R 원문의 "MoviePy" 명시와는 다르지만, 자막 요구사항이 확장된 시점에서 기술적으로 더 적합한 선택입니다.

### 4-2. 자막 스타일 범위: MVP vs 확장 목표

실제 상용 편집 프로그램(캡컷, 프리미어 등)의 자막 기능과 비교했을 때, FFmpeg/ASS로 "거의 동일하게" 구현 가능한 것과 "원리적으로 어려운" 것을 구분했습니다.

| 구분 | 항목 |
|---|---|
| **MVP (구현)** | 자동 줄바꿈, 폰트/색상/크기, 외곽선/그림자, 배경 박스(필박스), 카라오케 하이라이트(단어 단위 색상 변화), 페이드인/아웃 |
| **확장 목표 (후속 과제)** | 팝업/바운스 애니메이션, 곡선 텍스트, 3D 회전/틸트, 글로우/네온 효과 |

**근거**: MVP 항목은 ASS 네이티브 태그(`\k`, `BorderStyle=3`, `\fad` 등)로 직접 지원되어 리스크가 낮습니다. 반면 확장 목표 항목은 프레임별 이미지를 직접 계산/합성해야 하는 수준이라 캡스톤 일정 안에서 시도하면 일정 지연 위험이 큽니다. 특히 카라오케 하이라이트는 Whisper의 단어 단위 타임스탬프만 있으면 자동화가 가능해 구현 난이도 대비 효과가 커서 MVP에 포함했습니다.

### 4-3. 편집 JSON 스키마: `cuts` (유지할 구간) 방식

편집 정보를 "삭제할 구간(deletions)"이 아니라 "유지할 구간(cuts)"으로 표현하기로 확정했습니다.

**근거 (프론트엔드 UI 세부사항과 무관한 순수 기술적 이유)**:
1. FFmpeg의 `concat demuxer`가 원래 "남길 구간 리스트"를 입력으로 받는 도구라, `cuts`를 받으면 변환 없이 그대로 사용 가능합니다.
2. `deletions`로 받으면 겹치는 구간, 정렬 안 된 구간, 경계값(0초/끝) 처리 등 엣지케이스 방어 로직을 미디어 엔진이 직접 짜야 합니다.
3. 자막 타임스탬프 재계산(`timestamp_mapper.py`) 단계도 어차피 "최종적으로 남는 구간"을 알아야 하므로, 파이프라인 내부적으로는 `cuts` 형태가 필요합니다. `deletions`로 받아도 결국 내부에서 한 번은 변환해야 하는 건 동일합니다.
4. 실제 상용 NLE(프리미어, 캡컷 등)도 내부적으로 "삭제된 구간 목록"이 아니라 "현재 타임라인에 남아있는 클립들의 리스트(각각 원본 기준 in/out point)"를 데이터로 저장합니다. `cuts` 스키마는 이 표준 데이터 모델과 일치합니다.

**참고**: 이 스키마는 미디어 엔진(팀원4) 입장에서의 권장안이며, 프론트엔드(팀원1)와 최종 협의가 필요합니다. 다만 위 4번 근거처럼 실제 편집 프로그램의 표준 데이터 모델과 일치한다는 점에서, UI가 어떤 식으로 구성되든(AI 패널이 별도로 있는 구조라도) 메인 타임라인 데이터 자체는 `cuts` 형태로 자연스럽게 나올 가능성이 높습니다.

### 4-4. 키프레임 이슈 fallback: 전체 재인코딩 (MVP)

FFmpeg `-c copy`(무손실 스트림 복사)는 컷 포인트가 키프레임 경계와 맞지 않으면 실패하거나 프레임이 밀릴 수 있습니다.

- **A안 (채택)**: 실패 시 전체를 재인코딩. 단순하고 항상 안전하지만 느림.
- **B안 (확장 목표)**: 문제되는 컷 지점만 부분 재인코딩(키프레임 탐지 후 GOP 단위 처리). 빠르지만 구현 난이도가 높음.

**근거**: 캡스톤 일정을 고려해 MVP는 A안으로 가고, 시간이 남으면 B안으로 최적화하는 것으로 결정했습니다.

### 4-5. 비동기 처리: BackgroundTasks + 스레드풀 (Celery 미사용)

렌더링은 수십 초~몇 분이 걸리는 무거운 작업이라, FastAPI의 비동기 이벤트 루프를 막지 않도록 별도 처리가 필요합니다.

- **A안 (미채택)**: Celery + Redis 등 정식 작업 큐. 프로덕션급 안정성과 확장성을 갖지만, Redis 설치/운영, 워커 프로세스 관리 등 인프라 부담이 큼.
- **B안 (채택)**: FastAPI `BackgroundTasks` + 스레드풀(`run_in_threadpool`). 추가 인프라 없이 이벤트 루프 블로킹을 방지.

**근거**: 캡스톤 시연/소규모 테스트 규모에서는 Celery가 과설계입니다. 팀원2가 인프라 세팅에 시간을 쓰기보다 API 설계·예외 처리·웹소켓 같은 핵심 작업에 집중하는 것이 팀 전체 리소스 배분상 유리합니다. `render_video()`를 순수 블로킹 함수로 깔끔하게 짜두면, 추후 정말 필요할 때 호출부만 Celery로 교체하는 확장도 가능합니다.

---

## 5. 팀원2(백엔드)와의 연동 인터페이스

### 5-1. 진입점 함수 시그니처

```python
# main.py — 팀원2가 호출하는 유일한 진입점
def render_video(
    source_path: str,
    edit_data: dict,
    progress_callback: Callable[[str, int], None]
) -> str:
    """
    edit_data = {
        "cuts": [{"start": float, "end": float}, ...],
        "subtitles": [
            {
                "start": float,
                "end": float,
                "text": str,
                "words": [{"word": str, "start": float, "end": float}, ...]  # 카라오케용
            }
        ],
        "style_preset": "매운맛" | "순한맛" | "정석맛"
    }

    progress_callback(stage: str, percent: int) -> None
        stage 예: "cutting" | "generating_subtitles" | "encoding" | "done"

    반환값: 최종 렌더링된 영상의 절대경로 (str)
    실패 시: exceptions.py에 정의된 MediaEngineError 계열 예외 발생
    """
```

이 함수는 **순수 블로킹 함수**로 작성합니다. 비동기 처리 책임은 팀원2(호출부)에 있습니다.

### 5-2. 호출 방식 (팀원2 담당)

- `/render` 요청 수신 시, `run_in_threadpool(render_video, ...)` 형태로 스레드풀에서 실행
- 렌더링 중에도 다른 API 요청이 정상 처리되도록 이벤트 루프를 막지 않음
- `progress_callback`은 팀원2가 웹소켓 전송 함수로 감싸서 주입 (의존성 주입 패턴)
  - 스레드에서 웹소켓(비동기)으로 데이터를 보내야 하므로, `asyncio.run_coroutine_threadsafe` 등으로 스레드-이벤트루프 간 안전한 통신 처리 필요 (팀원2 담당 영역)

### 5-3. 진행률 단계 분배

컷 편집(`-c copy` 성공 시 거의 즉시)과 자막 합성/인코딩(시간 대부분 차지)의 소요 시간 비율을 반영해 불균등하게 분배합니다.

```python
progress_callback("cutting", 5)
progress_callback("generating_subtitles", 15)      # .ass 파일 생성은 빠름
progress_callback("encoding", 20)                    # 인코딩 시작
# 인코딩 도중: 20~95% 구간에서 갱신
progress_callback("done", 100)
```

- **MVP**: 단계 전환 시점마다 고정 % 전달
- **확장 목표**: FFmpeg가 stderr로 출력하는 `time=00:01:23.45` 진행 정보를 파싱해 20~95% 구간에 정교하게 매핑

### 5-4. 에러 전파

`exceptions.py`에 커스텀 예외 클래스를 정의하고, 팀원2가 이를 catch하여 사용자에게 표시할 메시지로 매핑합니다.

| 예외 클래스 | 발생 상황 |
|---|---|
| `InvalidCutRangeError` | cuts 구간이 겹치거나, 역전되거나, 영상 길이를 벗어남 |
| `FFmpegCuttingError` | 컷 편집 단계 실패 (전체 재인코딩 fallback도 실패한 경우) |
| `SubtitleGenerationError` | .ass 파일 생성 실패 (폰트 없음, 스타일 프리셋 오류 등) |
| `FFmpegEncodingError` | 최종 인코딩 단계 실패 |

---

## 6. 팀원3(AI 파이프라인)과의 연동점

- `style_presets.py`에 정의된 "매운맛/순한맛/정석맛" 프리셋은 팀원3의 스타일 분석 알고리즘 출력값과 이름이 일치해야 합니다.
- 각 프리셋은 자막 폰트/색상/크기/외곽선 조합을 딕셔너리로 정의하며, `ass_generator.py`가 이를 참조해 실제 ASS 스타일 태그로 변환합니다.
- 카라오케 하이라이트를 위해서는 팀원3(또는 Whisper 연동 담당)이 **단어 단위 타임스탬프**를 포함한 형태로 `subtitles` 데이터를 넘겨줘야 합니다 (위 5-1의 `words` 필드).

---

## 7. 팀원1(프론트엔드)과 협의가 필요한 사항

- **`cuts` 스키마 최종 확정**: §4-3에서 설명한 근거를 바탕으로 미디어 엔진 측 권장안은 `cuts`(유지 구간)입니다. 다만 실제 UI 인터랙션 구현 방식에 따라 프론트엔드에서 더 자연스러운 형태가 다를 수 있으므로, 이 문서의 근거를 공유하고 최종 협의가 필요합니다.

---

## 8. 확장 목표 (MVP 이후 우선순위 낮은 순서로)

1. FFmpeg stderr 파싱 기반 실시간 인코딩 진행률
2. 키프레임 부분 재인코딩(GOP 단위) 최적화
3. 자막 팝업/바운스 애니메이션
4. 곡선 텍스트, 3D 회전, 글로우 효과
