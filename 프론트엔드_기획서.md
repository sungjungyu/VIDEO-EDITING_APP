# 프론트엔드 (팀원1) 구현 현황 및 기획서

> 이 문서는 `static/editor.html` 의 현재 구현 상태를 정리한 것입니다.
> 다른 팀원 또는 다른 Claude 인스턴스가 프론트엔드를 이어서 개발하거나 연동할 때 참고하십시오.

---

## 1. 파일 구조

```
static/
└── editor.html   ← 프론트엔드 전체 (단일 파일, 빌드 도구 없음)
```

- **빌드 도구 없음**: Webpack/Vite 등 번들러를 사용하지 않습니다.
- **외부 의존성**: Tailwind CSS CDN (`https://cdn.tailwindcss.com`) — 유틸리티 클래스 보조용이며 핵심 레이아웃은 모두 커스텀 CSS 변수 기반입니다.
- **프레임워크 없음**: 순수 HTML + CSS + Vanilla JS로 작성됩니다.
- FastAPI의 `GET /` 라우트가 이 파일을 읽어 그대로 반환합니다 (`app.py` 의 `home()` 함수).

---

## 2. 레이아웃 구조

```
┌──────────────────────────────────────────────┐
│  TopBar: 브랜드 로고 / [내보내기] 버튼         │  height: 56px
├───────────────────────────┬──────────────────┤
│  영상 미리보기 영역         │  AI 수정 요청     │  flex: 1
│  - 영상 선택 버튼           │  채팅 패널        │
│  - 스타일 선택 (select)     │  (우측 고정 폭)   │
│  - AI 분석 시작 버튼        │                  │
│  - 파일명 / 진행 상태       │                  │
│  - <video> 플레이어         │                  │
│  - 재생/탐색 컨트롤         │                  │
├───────────────────────────┴──────────────────┤
│  타임라인 패널 (가로 스크롤)                    │  height: 고정
│  - 눈금자 (ruler)                              │
│  - 세그먼트 블록 트랙 (track)                  │
│  - 플레이헤드 (드래그 가능)                    │
└──────────────────────────────────────────────┘
      ↕ 별도 레이어
┌─────────────────────────────────────────────┐
│  내보내기 모달 (hidden, 필요 시 표시)          │
└─────────────────────────────────────────────┘
```

---

## 3. 디자인 시스템 (CSS 변수)

```css
--bg:     #12161C   /* 최외곽 배경 */
--panel:  #1B222B   /* 패널 배경 */
--border: #2A3340   /* 테두리 */
--text:   #E8ECF1   /* 기본 텍스트 */
--muted:  #9AA6B2   /* 보조 텍스트 */
--accent: #FFD84D   /* 강조 (노란색) — 버튼, 로고 그라디언트 등 */
--keep:   #5CA8FF   /* 살리는 구간 (파란색) */
--cut:    #FF5C5C   /* 자르는 구간 (빨간색) */
```

---

## 4. 핵심 데이터 구조

### 4-1. `SEGMENTS` 배열 (`let`)

```js
let SEGMENTS = [
  { start: 0.0, end: 3.2, text: "안녕하세요...", cut: false },
  { start: 3.2, end: 4.1, text: "어...",          cut: true  },
  // ...
];
```

- **`start` / `end`**: 원본 영상 기준 초 단위 타임스탬프
- **`text`**: 해당 구간의 자막/발화 텍스트
- **`cut`**: `true` = 잘라낼 구간, `false` = 살릴 구간
- AI 분석 결과 수신 시 배열 전체를 교체합니다 (`SEGMENTS = data.segments`)
- 페이지 로드 시 초기값은 하드코딩된 목업 데이터 (영상 미선택 상태에서도 타임라인이 보임)

### 4-2. 상태 변수

```js
let SOURCE_FILE = null;  // /analyze 응답의 source_file 토큰 — /render 호출 시 사용
let currentFile  = null; // 사용자가 선택한 File 객체 (로컬 blob 미리보기 + 서버 업로드용)
```

---

## 5. 전체 사용자 흐름

```
1. 영상 선택 (input[type=file])
      → 로컬 Blob URL 생성, <video> 소스로 설정 (미리보기)
      → currentFile 저장, btnAnalyze 활성화
      → SOURCE_FILE = null (이전 분석 무효화)

2. 스타일 선택 (select#style-select)
      → "정석맛" / "매운맛" / "순한맛" 중 선택
      → /analyze 호출 시 style 파라미터로 전달

3. AI 분석 시작 버튼 클릭
      → POST /analyze (multipart: file + style)
      → 즉시 job_id 수신
      → 1.5초마다 GET /analyze_jobs/{job_id} 폴링
      → 파일명 옆에 진행바 + 단계명 표시
            예) [██░░░░░░░░] 15%  음성 인식(Whisper) 처리 중...
      → status === "completed" 시:
            SEGMENTS = job.segments
            SOURCE_FILE = job.source_file
            renderTimeline(SEGMENTS)
            updateStats(), updateTimelineWidth(), renderRuler()

4. 타임라인 조작
      → 세그먼트 블록 클릭: cut 토글 (keep ↔ cut)
      → 세그먼트 블록 더블클릭: 텍스트 인라인 편집
      → 눈금자/플레이헤드 클릭·드래그: 비디오 탐색

5. AI 수정 요청 (우측 채팅 패널)
      → 자연어 입력 후 전송
      → POST /revise { instruction, segments: SEGMENTS }
      → 응답의 segments로 SEGMENTS 교체 후 타임라인 재렌더

6. 내보내기 버튼 → 모달 열림
      → buildExportPayload(SEGMENTS) = segments 배열 표시 (JSON 미리보기)
      → "백엔드로 보내기" 클릭:
            SOURCE_FILE 없으면 오류 표시
            POST /render { source_file, segments, style_preset: "정석맛" }
            즉시 job_id 수신 → 1.5초마다 GET /jobs/{job_id} 폴링
            status === "completed" 시 다운로드 링크 표시
```

---

## 6. 백엔드 API 연동 요약

| 엔드포인트 | 호출 시점 | 요청 | 응답 |
|---|---|---|---|
| `POST /analyze` | AI 분석 버튼 클릭 | `multipart/form-data` (file, style) | `{ job_id, status }` |
| `GET /analyze_jobs/{job_id}` | 분석 폴링 (1.5초마다) | — | `{ status, stage, progress, segments?, source_file? }` |
| `POST /revise` | AI 채팅 전송 | `{ instruction, segments }` | `{ ok, message, segments? }` |
| `POST /render` | 모달 내 렌더 버튼 | `{ source_file, segments, style_preset }` | `{ job_id, ... }` |
| `GET /jobs/{job_id}` | 렌더 폴링 (1.5초마다) | — | `{ status, progress, message, output_url? }` |
| `GET /media/{filename}` | `<video>` src (분석 완료 후, 미사용) | — | 영상 스트림 |
| `GET /download/{filename}` | 렌더 완료 다운로드 링크 | — | 영상 파일 |

> **현재 미사용**: 분석 완료 후 `source_url`(`/media/{filename}`)을 `<video>` src로 교체하는 로직이 없습니다.
> 현재는 로컬 Blob URL이 그대로 유지됩니다. 개선 여지가 있습니다.

---

## 7. 주요 JS 함수 목록

| 함수 | 역할 |
|---|---|
| `renderTimeline(segments)` | 트랙에 세그먼트 블록 전체 재렌더 |
| `renderRuler()` | 눈금자 재렌더 (5초 단위 major tick) |
| `updateTimelineWidth()` | 타임라인 가로 폭 재계산 |
| `updateStats()` | 상단 통계(유지/제거 수, 예상 길이) 갱신 |
| `toggleSegment(i, el)` | 세그먼트 cut 토글 (DOM 부분 갱신) |
| `startEditing(i, el)` | 세그먼트 텍스트 인라인 편집 시작 |
| `stopEditing(i, el)` | 인라인 편집 종료 및 반영 |
| `totalDurationSec()` | SEGMENTS 마지막 end와 video.duration 중 큰 값 |
| `analyzeVideo()` | /analyze 호출 + 진행률 폴링 + 타임라인 갱신 |
| `requestAiEdit(instruction)` | /revise 호출 |
| `submitRender(payload, onProgress)` | /render 호출 + /jobs 폴링 |
| `buildExportPayload(segments)` | SEGMENTS → 렌더 요청 형태 배열 변환 |
| `updatePlaybackUI()` | 재생 시간 / 프로그레스바 / 플레이헤드 갱신 |

---

## 8. 타임라인 스케일

- `PX_PER_SECOND = 60` : 1초 = 60px (눈금자·세그먼트·플레이헤드 공통)
- 세그먼트 블록: `left = start * 60px`, `width = (end - start) * 60px`
- 눈금자: 1초마다 minor tick, 5초마다 major tick + 시간 레이블
- 타임라인 전체 폭: `totalDurationSec() * 60 + 40(여유)` px

---

## 9. 미구현 / 개선 여지

1. **분석 완료 후 서버 영상으로 `<video>` src 교체** — 현재 로컬 Blob 유지
2. **스타일 프리셋을 렌더 시 UI에서 선택** — 현재 렌더는 항상 `"정석맛"` 고정
3. **AI 채팅 응답에서 segments가 있을 때 자동 타임라인 갱신** — 현재 구현됨, 추가 UX 피드백(강조 등) 없음
4. **모바일 반응형** — 현재 데스크톱 전용 레이아웃
5. **영상 없이 AI 분석 불가 안내** — `btnAnalyze` disabled 상태로 처리됨
